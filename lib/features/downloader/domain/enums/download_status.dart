// Path: lib/features/downloader/domain/enums/download_status.dart

// Enum to define the status of a download
enum DownloadStatus {
  pending,
  downloading,
  paused,
  completed,
  failed,
  cancelled,
}